def fun2():
    class G:
        private "KLATA_PUDZIANA()"
    return G

A = fun2()


list = [A, B, C]

def fun(list, i):
    if list.len < i:
        return list
    list[i] -> list[i+1]
    list = fun(list, i+1)

list = fun(list, 0)




def fun(arg):
    arg = fun(arg)
    arg -> global&A
    return arg

diagclass PUDZIAN_KING:
    class test:
        private "test()"
    X = fun(test)
    exec X
